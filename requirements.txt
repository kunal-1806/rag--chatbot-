# ---------------------------------------------------------------------------
# RAG chatbot project - requirements.txt
# Tested target: Python 3.11 - 3.13 (M1/M2/M3 Mac and Linux)
# Install with: pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Core orchestration ---
langchain>=1.3.14
langgraph>=1.2.9
langchain-community>=0.4.0
langchain-ollama>=0.4.0          # local LLM + embeddings via Ollama
langchain-nvidia-ai-endpoints>=0.4.0   # NVIDIA NIM integration
langchain-openai>=0.4.0          # used for OpenRouter (OpenAI-compatible endpoint)
langchain-openrouter>=0.0.3      # OpenRouter LLM integration

# --- Vector DB ---
chromadb>=1.5.9
faiss-cpu>=1.10.0

# --- Embeddings (local, if not using Ollama's own) ---
sentence-transformers>=3.4.0

# --- Ingestion: PDF ---
pymupdf>=1.25.0                  # fitz - fast PDF text/layout extraction
unstructured[pdf]>=0.17.0        # fallback for complex/multi-column PDFs
pytesseract>=0.3.13              # OCR for scanned PDFs (requires system tesseract)

# --- Ingestion: YouTube ---
youtube-transcript-api>=1.0.0
yt-dlp>=2025.1.1                 # fallback: download audio if no captions exist

# --- Ingestion: video/audio transcription ---
faster-whisper>=1.1.0            # CTranslate2-based Whisper, fast on CPU/Apple Silicon
ffmpeg-python>=0.2.0             # audio extraction from video files

# --- Chunking / text utils ---
tiktoken>=0.9.0                  # token counting for chunk sizing

# --- UI ---
streamlit>=1.42.0

# --- Utilities ---
python-dotenv>=1.0.1              # load API keys from .env
pydantic>=2.10.0
requests>=2.32.0
numpy>=2.1.0

# ---------------------------------------------------------------------------
# NOTE: You also need `tesseract` and `ffmpeg` installed as system binaries
# (not pip packages). On macOS:
#   brew install tesseract ffmpeg
#
# NOTE: For local LLM/embeddings, install Ollama separately (not a pip
# package): https://ollama.com/download - then `ollama pull llama3.1:8b`
# and `ollama pull nomic-embed-text`.
# ---------------------------------------------------------------------------
